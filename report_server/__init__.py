"""B-PILOT experiment-report viewer service.

Deliberately self-contained: this package must never import ``B_PILOT``, and it
is never installed into the ``bpilot_mpe_dev`` environment. See ``README.md``.
"""
