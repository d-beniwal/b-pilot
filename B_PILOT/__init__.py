"""B-PILOT (Bluesky-PILOT): Qt plan-runner GUI, currently for the MPE
beamlines (Sectors 1 / 20).

Lives at ``<workspace>/b-pilot/B_PILOT`` (a sibling of the ``mpe_bluesky``
checkout since 2026-09-02, not nested inside it).  Docstring-driven plan form with a
live embedded IPython console panel.  See ``app.py`` (or the top-level
``launch.py``) for the entry point and ``B_PILOT.paths`` for how it locates the
rest of the host project.
"""
